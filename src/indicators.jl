# Curated shortcuts — four or five, no more.
#
#   industrial_production (115_333), gdp (163_156),
#   consumer_prices (167_744), unemployment (151_874)
#
# Each is a hand-maintained mapping that goes stale when ISTAT revises a
# dataflow, so each needs an opt-in live test and a docstring naming the
# dataflow so users can drop to get_data when one drifts.
